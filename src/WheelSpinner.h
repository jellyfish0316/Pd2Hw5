#include <iostream>
#include <vector>
#include <cstdlib>
#include <ctime>
#include <thread>
#include <chrono>
using namespace std;

void show_wheel(const vector<string>& problems, int current);
int spin_wheel(const vector<string>& problems);

